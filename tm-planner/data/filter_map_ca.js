var filter_map_ca = {
    'slot-pos':                 /orbs beneficial/i,
    'bind-red':                 /(reduces|removes).+bind.+duration/i,
    'desp-red':                 /(reduces|removes).+despair.+duration/i,
    'cd-red':                   /restores.+special cooldowns?.+rewinded/i,
    'silence-red':              /(reduces|removes).+silence.+duration/i,
    'para-red':                 /(reduces|removes).+paralysis.+duration/i
};
